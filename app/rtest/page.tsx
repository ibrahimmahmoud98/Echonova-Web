// TEMP-TEST ROUTE — created only to screenshot-test the TheatreStage
// responsive fix in the sandbox. DELETE this whole folder before finishing.
import { TheatreStage } from "@/components/sections/TheatreStage";

export default function RTestPage() {
  return (
    <main className="min-h-screen bg-[var(--color-navy)]">
      <TheatreStage />
    </main>
  );
}
