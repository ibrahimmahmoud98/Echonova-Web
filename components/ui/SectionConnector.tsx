/**
 * DEPRECATED — generic section connectors were the wrong abstraction.
 *
 * Real cinematic continuity requires per-section custom transitions
 * that use each section's own visual language (e.g. the Story lens
 * iris-wipes into the next chapter; MethodologyScroll's horizontal
 * pin slides ContactPageReveal in laterally).
 *
 * This file is kept empty to avoid breaking any stale references; it
 * intentionally exports nothing.
 */
export {};
